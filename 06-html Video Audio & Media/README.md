# HTML Video, Audio & Media 🎥🔊

This project demonstrates how to use **HTML media elements** such as video, audio, and embedded external content using an iframe.

---

## 📁 Files Included

- **index.html** – Contains HTML code for video, audio, and embedded media
- **samplevideo.mp4** – Video file used in the `<video>` tag (if available)

---

## 📘 Media Elements Used

## 🎬 Video
The `<video>` tag is used to display video content on a webpage with built-in controls.

**Features used:**
- `controls` – Shows play/pause controls
- `loop` – Repeats the video
- `muted` – Starts the video without sound
- `height` – Sets video height

**Example:**
```html
<video src="samplevideo.mp4" height="255" controls loop muted></video>
```
## 🔊 Audio

The `<audio>` tag is used to play audio files on a webpage.

**Example:**
```html
<audio src="" controls></audio>
```

## 📺 Embedded Media (YouTube)

The `<iframe>` tag is used to embed external media such as YouTube videos.

**Example:**
```html
<iframe
    width="560"
    height="315"
    src="https://www.youtube.com/embed/XZwBNDGuWGU?start=790"
    allowfullscreen>
</iframe>
```

## 🛠️ Technologies Used

HTML5

## 🎯 Learning Outcomes

- Embed video and audio using HTML5

- Use media attributes like controls, loop, and muted

- Embed external content using iframe

- Understand basic media handling in web pages

## 🚀 Author

Ishand Rai
Frontend Web Development Learner

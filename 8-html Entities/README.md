# HTML Entities 🧩

This project demonstrates the use of **HTML entities** to display reserved characters, special symbols, and formatted spaces in a web page.

---

## 📁 Files Included

- **entities.html** – Contains examples of commonly used HTML entities

---

## 📘 HTML Entities Used

### 🔹 Reserved Characters
HTML entities are used to display characters that have special meaning in HTML.

| Entity | Output | Description |
|------|-------|-------------|
| `&lt;` | `<` | Less than symbol |
| `&gt;` | `>` | Greater than symbol |
| `&amp;` | `&` | Ampersand |

---

### 🔹 Non-Breaking Space
The `&nbsp;` entity is used to insert extra spaces that are not collapsed by the browser.

**Example:**
```html
hello &nbsp;&nbsp;&nbsp;&nbsp; world
```
### 🔹 Copyright Symbol

The `&copy;` entity is used to display the copyright symbol.

**Example:**
```html
&copy;
```

### 🔹 Preformatted Text

The `<pre>` and <code> tags are used to preserve whitespace and formatting while displaying code.

**Example:**
```html
<pre><code>
&lt;p&gt;Hello World&lt;/p&gt;
</code></pre>
```

## 🛠️ Technologies Used

- HTML5

## 🎯 Learning Outcomes

- Understand what HTML entities are

- Display reserved HTML characters safely

- Use non-breaking spaces for formatting

- Show code blocks using `<pre>` and `<code>`


### 🚀 Author

Ishand Rai
Frontend Web Development Learner


# HTML Elements Practice 🧩

This project demonstrates the use of common **HTML elements** such as images, tables, lists, and links. It is created for practicing basic HTML structure and tags.

---

## 📁 Files Included

- **index.html** – Main HTML file containing all elements
- **style.css** – External stylesheet (linked)
- **script.js** – External JavaScript file (linked)
- **image.png** – Image used in the webpage

---

## ✨ Features & HTML Concepts Used

- Image display using `<img>` tag
- Tables with:
  - `<caption>`
  - `<thead>`, `<tbody>`, `<tfoot>`
  - `<tr>`, `<th>`, `<td>`
- Ordered and unordered lists:
  - `<ul>` (default & square type)
  - `<ol>` (different list styles)
- Image used as a clickable link
- Meta tags for viewport and description
- External CSS and JavaScript linking

---

## 🛠️ Technologies Used

- HTML5
- CSS3 (linked)
- JavaScript (linked)

---

## ▶️ How to Run

1. Download or clone the project folder
2. Make sure all files (`index.html`, `style.css`, `script.js`, `image.png`) are in the same directory
3. Open `index.html` in any web browser

---

## 🎯 Learning Outcomes

- Understanding HTML page structure
- Creating tables with semantic sections
- Using different types of lists
- Adding images and making them clickable
- Linking external resources (CSS & JS)

---

## 🚀 Author

**Ishand Rai**  
Frontend Web Development Learner

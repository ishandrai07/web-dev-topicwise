# CSS Shadows & Outline ✨

This project demonstrates the use of **CSS box-shadow, text-shadow, outline, and outline-offset** to enhance the visual appearance of HTML elements using **internal CSS**.

---

## 📁 Files Included

- **index.html** – Contains HTML structure and internal CSS for shadows and outlines

---

## 🎨 CSS Type Used

✅ **Internal CSS**  
All CSS is written inside the `<style>` tag within the `<head>` section of the HTML file.

---

## 📘 Concepts Covered

### 🔹 Box Shadow
The `box-shadow` property is used to apply shadow effects around an element’s box.

**Example:**
```css
box-shadow: 5px 5px 10px 4px grey;
```
## 🔹 Text Shadow

The `text-shadow` property adds shadow effects to text.
Single shadow:
**Example:**
```css
text-shadow: 5px 5px 5px gray;
```
Multiple shadows (Glow Effect):
```css
text-shadow: 
    0 0 10px rgb(56, 26, 110),
    0 0 20px rgb(23, 153, 41),
    0 0 30px #00f;
```

## 🔹 Outline & Outline Offset

The `outline` property draws a line outside the border of an element.
**Example:**
```css
outline: 2px solid red;
outline-offset: 2px;
```

## 🧪 Layout Explanation

- Box 1 uses `box-shadow` to create depth

- Text container uses:

  - `text-shadow` for glow effect

  - `border-radius` for rounded corners

  - `outline` for emphasis

  - `outline-offset` to separate outline from border

  ---
## 🛠️ Technologies Used

- HTML5

- CSS3

---
## 🎯 Learning Outcomes

- Understand how box-shadow works

- Apply text glow effects using text-shadow

- Learn the difference between border and outline

- Use internal CSS effectively

- Improve UI styling with shadows
---
## 🚀 Author

Ishand Rai
Frontend Web Development Learner

